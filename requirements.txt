# === Núcleo LLM / RAG (versões fixadas para evitar backtracking do pip) ===
langchain==0.3.7
langchain-core==0.3.29
langchain-text-splitters==0.3.2
langchain-community==0.3.7
langchain-groq==0.2.3
langchain-huggingface==0.1.2

# === Vector store & embeddings ===
chromadb==0.5.20
sentence-transformers==3.3.1

# === Stack de ML fixada (compatível com numpy 1.26 / Python 3.11) ===
# IMPORTANTE: estas versões DEVEM permanecer fixadas juntas. Deixar torch,
# transformers, numpy, scipy ou scikit-learn soltos faz o pip puxar versões
# recentes (torch 2.14 / transformers 4.57 / sklearn 1.9 / scipy 1.17) que
# exigem numpy 2.x e travam o import (incompatibilidade de ABI das extensões C).
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
torch==2.2.2
transformers==4.44.2
tokenizers==0.19.1
huggingface-hub==0.25.2

# === Provider (Groq) ===
groq==0.13.1

# === Leitura de documentos ===
pypdf==5.1.0

# === Interface / CLI ===
streamlit==1.40.2
rich==13.9.4

# === Dados & métricas ===
pandas==2.2.3
plotly==5.24.1

# === Utilidades ===
python-dotenv==1.0.1
tenacity==9.0.0
